const styles = (theme) => ({
    taskboard: {
        display: 'flex',
        alignItems: 'center'
    },
    shape: {
        borderRadius: 4,
        margin: 10,
        padding: 20,
        backgroundColor: theme.color.primary,
        color: 'white',
        borderColor: '#cccccc',
    }
});

export default styles;